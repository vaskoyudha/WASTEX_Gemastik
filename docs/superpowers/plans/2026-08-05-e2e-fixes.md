# Fix Plan: E2E-Discovered Production Issues (RLS recursion, web login, test/tsc hygiene)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fix the 4 issues discovered during E2E testing via Expo web + Playwright (2026-08-05), in priority order. Nothing here changes the additional-materials feature behavior — these are production-readiness and test-hygiene fixes.

**Background (evidence from E2E session):**
- `POST /skills` and `GET /tutorial/{id}` intermittently 500'd with `infinite recursion detected in policy for relation "users"` (code 42P17) — appeared right after the `supabase db push` of `20260805000001/0002` migrations, then stopped after server restarts. Possibly a transient PostgREST schema-cache race after DDL, or a real recursive RLS policy on `users`. Must be investigated deterministically.
- Real Supabase login tokens are **ES256** (`eyJhbGciOiJFUzI1NiIsImtpZ...`) but `backend/app/auth.py:get_current_user` only decodes **HS256** with the local `supabase_jwt_secret` → every authenticated endpoint rejects real login tokens with `401 Invalid token`.
- Frontend `src/services/auth/index.ts:signIn` calls `supabase.auth.signInWithPassword` against `EXPO_PUBLIC_SUPABASE_URL` (defaults to `http://localhost:54321`, not running in web dev) and **throws before** the backend `/auth/login` fallback → login unusable in web.
- `app/(tabs)/profil.test.tsx` has 1 pre-existing failing test (`mySkills.length` on undefined).
- `npx tsc --noEmit` has 5 pre-existing errors (after `b95b188` fixed one): profil.tsx ×3 (174, 243, 250), Input.tsx (16), auth index.test.ts (31).

## Global Constraints

- Ruff: line-length 100; CI gates `uv run ruff check backend/` then `uv run ruff format --check backend/` (run from repo root: `backend/.venv/bin/ruff ...`). Backend suite: `uv run pytest tests/` from `backend/` (currently 170 passed, 4 skipped). Frontend: `npx jest src app --silent` — after Task 3, **0 failures** (profil.test.tsx must pass); `npx tsc --noEmit` — after Task 4, **0 errors**.
- Migrations idempotent, numbered `YYYYMMDD*_*.sql` in `backend/supabase/migrations/`. The remote Supabase project (`ibxnycomuwbloqaninji`) is linked (`supabase link` done); apply via `supabase db push` from repo root — **note: the CLI reads `supabase/migrations/`; create the symlink `ln -s ../backend/supabase/migrations supabase/migrations` first** (this was how the previous push succeeded; it is untracked scratch, do not commit).
- Backend `.env` has `SUPABASE_SERVICE_KEY` and `SUPABASE_JWT_SECRET`; `create_test_token()` (HS256, `app.auth`) is the test path. Real Supabase tokens are ES256 — do not weaken that.
- Indonesian UI copy; TypeScript strict; commits to `main` per AGENTS.md. Do NOT commit `.gitignore` (has pre-existing `visuals/` mod) or the `supabase/migrations` symlink.
- A uvicorn dev server is running on :8000 and Expo web on :8081 — you may restart uvicorn during verification but do not leave the repo in a broken state.

---

### Task 1: Diagnose + fix the `users` RLS recursion (remote)

**Files:**
- Create (if a real policy fix is needed): `backend/supabase/migrations/20260806000001_fix_users_rls_recursion.sql`
- Possibly modify: nothing in app code.
- Evidence/notes: `backend/tests/test_rls_regression.py` only if a deterministic unit-level check is possible; otherwise document in report.

**Interfaces:**
- Consumes: remote project access via `supabase link` + `supabase db push` (symlink trick above).
- Produces: a remote schema where `GET /tutorial/{id}`, `POST /skills`, `POST /skills/proposals`, `POST /skills/verify` never 500 with 42P17; a migration (if a policy is the cause) committed to the repo.

- [ ] **Step 1: Reproduce deterministically.** From repo root, with the linked project and running uvicorn, hit the endpoints repeatedly (e.g. `for i in $(seq 1 20); do curl -s -o /dev/null -w "%{http_code} " http://localhost:8000/tutorial/b0fe87c2-acfb-4ef1-b30c-78148329a0ba; done` and the same for `POST /skills` dup-check via `create_test_token` with a real user UUID `sub`). Record the failure rate and the exact backend log line. If it does not reproduce after N attempts, note that and proceed to Step 2 to confirm the mechanism.
- [ ] **Step 2: Inspect remote RLS policies.** Try `supabase db dump --linked --schema public` (may need `--password`; the CLI may already have stored credentials — if it prompts, check `supabase link` state and try `supabase db push --dry-run` which earlier worked without extra auth). Identify any policy on `skills`, `users`, or `profiles` that references the table it is on (self-referencing policy = recursion), especially anything like `using (auth.uid() in (select id from users ...))` or a policy that references `users` from `skills`.
- [ ] **Step 3: Fix.** If a recursive policy exists: write `20260806000001_fix_users_rls_recursion.sql` that drops/recreates the offending policy correctly (idempotent, `drop policy if exists ... on ...;` first). Apply with `supabase db push`. If the recursion is instead a transient PostgREST schema-cache artifact (no bad policy found): document that conclusion in the report with the reproduction evidence, and add no migration — the task then passes with evidence + no code change.
- [ ] **Step 4: Verify.** Re-run Step 1's loop; expect 100% non-500. Also verify `GET /pricing/{id}` and `GET /skills?status=approved` are clean. If a migration was added, run `uv run pytest tests/ -q --tb=short` from `backend/` (must stay 170 passed/4 skipped) and ruff gates from root.
- [ ] **Step 5: Commit.** Only if a migration was created: `git add backend/supabase/migrations/20260806000001_fix_users_rls_recursion.sql && git commit -m "fix(db): resolve RLS recursion on users relation"`. If the conclusion is "no policy defect, transient cache race", commit nothing but document thoroughly in the report.

---

### Task 2: Real Supabase JWT verification + web login fallback

**Files:**
- Modify: `backend/app/auth.py` (`get_current_user`)
- Modify: `backend/tests/test_auth_endpoints.py` or `backend/tests/test_auth.py` (add ES256-shaped token test)
- Modify: `src/services/auth/index.ts` (`signIn` fallback order)
- Modify: `src/services/auth/__tests__/index.test.ts` (fallback coverage)

**Interfaces:**
- Consumes: `supabase.Client` (service key) for verifying real JWTs; `create_test_token` (HS256) for the test path.
- Produces: `get_current_user` accepts BOTH the local HS256 test token and real Supabase ES256 tokens; frontend `signIn` falls back to backend `/auth/login` when local Supabase is unreachable.

- [ ] **Step 1: Write the failing test first.** Append to the backend auth test file: a test where `get_current_user` is exercised with a token that is NOT decodable as HS256 with the local secret (e.g. a token signed with a different key / malformed-but-JWT-shaped), asserting the flow reaches the Supabase `auth.get_user` path (FakeAuth already implements `get_user` sync — verify it returns the right user shape). Also a frontend test: `signIn` calls `apiClient.login` and builds a User when the supabase call rejects.
- [ ] **Step 2: Verify RED** for both new tests.
- [ ] **Step 3: Implement backend.** In `backend/app/auth.py`, `get_current_user`: first try the existing HS256 decode (local test secret — keep it). On `jwt.InvalidTokenError`/`ExpiredSignatureError`, fall back to verifying via Supabase: obtain the client (lazy import `from app.deps import get_supabase` inside the function to avoid circular imports — check current import graph first), call `sb.auth.get_user(token)` (sync, service key — real tokens validate against Supabase's keys), and map the returned user id into `{"user_id": ..., "email": ...}`. Preserve the existing 401 behaviors for genuinely invalid tokens. Mind circular imports (`app.deps` imports from `app.auth`).
- [ ] **Step 4: Implement frontend.** In `src/services/auth/index.ts` `signIn`: keep the supabase attempt, but on failure fall back to `apiClient.login({email, password})` (backend `/auth/login`, returns `{access_token, user_id, profile}`) and build the `User` from that response (token from `access_token`, id from `user_id`, profile mapped like the existing profile mapping). Never throw before the backend fallback.
- [ ] **Step 5: Verify GREEN.** Run `uv run pytest tests/test_auth_endpoints.py tests/test_auth.py -v` (whichever file exists) + full backend suite + `npx jest src/services/auth --silent` + full frontend suite (profil.test.tsx may still fail until Task 3 — note it) + `npx tsc --noEmit` (must not add errors beyond the pre-existing 5).
- [ ] **Step 6: Live smoke.** Restart uvicorn; `curl` login with the real user (`e2e@wastex.app` / password in the E2E session) and use the returned ES256 token on `GET /skills?mine=true` and `POST /skills` — expect 200/201, not 401.
- [ ] **Step 7: Commit.** `git add backend/app/auth.py backend/tests/test_auth_endpoints.py backend/tests/test_auth.py src/services/auth/index.ts src/services/auth/__tests__/index.test.ts && git commit -m "fix(auth): verify real Supabase JWTs and fall back to backend login on web"`

---

### Task 3: Fix failing `profil.test.tsx` (clear-data test)

**Files:**
- Modify: `app/(tabs)/profil.test.tsx` and/or `app/(tabs)/profil.tsx`

**Interfaces:**
- Consumes: current test failure `TypeError: Cannot read properties of undefined (reading 'length')` at profil.tsx:257 (`mySkills.length`).
- Produces: the `clear-data action › asks for confirmation before clearing data` test passes; full frontend suite green (this is the last failure).

- [ ] **Step 1: Reproduce.** Run `npx jest "app/(tabs)/profil.test.tsx"` (quote the path; the parentheses break zsh). Confirm the single failing test and the stack.
- [ ] **Step 2: Diagnose.** Read the test's mocks for the profil screen (store/useScanStore or service mocks feeding `mySkills`) and the component's `mySkills` source. Determine whether the defect is (a) the mock not providing `mySkills` (test bug) or (b) the component reading `mySkills.length` when the store shape can be undefined (component bug).
- [ ] **Step 3: Fix.** Prefer the minimal correct fix: if the store legitimately may not have the key, guard the component (`mySkills?.length > 0 && ...`); if the mock is wrong, fix the mock. Do NOT gut the assertion.
- [ ] **Step 4: Verify.** `npx jest "app/(tabs)/profil.test.tsx"` passes; `npx jest src app --silent` → **0 failures**; `npx tsc --noEmit` → still exactly 5 errors (no new ones; Task 4 owns the existing 5).
- [ ] **Step 5: Commit.** `git add "app/(tabs)/profil.test.tsx" "app/(tabs)/profil.tsx" && git commit -m "fix(profil): guard mySkills access and fix clear-data test"`

---

### Task 4: Zero the remaining 5 tsc errors

**Files:**
- Modify: `app/(tabs)/profil.tsx` (errors at 174, 243, 250)
- Modify: `src/components/ui/Input.tsx` (error at 16)
- Modify: `src/services/auth/__tests__/index.test.ts` (error at 31)

**Interfaces:**
- Consumes: current `npx tsc --noEmit` output — exactly these 5:
  - `profil.tsx(174,67)`: `'user.profile' is possibly 'null'`
  - `profil.tsx(243,19)` + `(250,19)`: Button `flex` prop not in `ButtonProps`
  - `Input.tsx(16,23)`: comparison between `'string | null | undefined'` and `'boolean'`
  - `auth/__tests__/index.test.ts(31,59)`: `boolean` not assignable to `void | Promise<void>`
- Produces: `npx tsc --noEmit` exits clean (0 errors).

- [ ] **Step 1: Baseline.** Run `npx tsc --noEmit 2>&1 | grep "error TS"` — confirm exactly the 5 above.
- [ ] **Step 2: Fix each.** profil.tsx: narrow/guard `user.profile` access (read the surrounding code — likely a `user?.profile` chain or an early return); for the Button props, check `Button`'s props interface (components/ui) and either add `flex?: boolean`/`flex?: number` to `ButtonProps` or remove the invalid `flex` prop (match how other screens pass it — check siblings). Input.tsx: fix the boolean-vs-string comparison per the actual intent. auth test: adjust the mock callback signature to return `Promise<void>`.
- [ ] **Step 3: Verify.** `npx tsc --noEmit` → exit 0, zero lines. `npx jest src app --silent` → all pass (profil test green from Task 3). Backend untouched — no pytest needed, but run ruff from root only if any backend file changed (it shouldn't).
- [ ] **Step 4: Commit.** `git add app/(tabs)/profil.tsx src/components/ui/Input.tsx src/services/auth/__tests__/index.test.ts && git commit -m "fix(tsc): resolve remaining type errors to a clean typecheck"`

---

### Task 5: Full verification gates

**Files:** None modified.

- [ ] **Step 1: Backend full suite** — `cd backend && uv run pytest tests/ -q --tb=short` → 170+ passed (grew only by Task 2 tests), 4 skipped.
- [ ] **Step 2: Ruff gates** — from root: `backend/.venv/bin/ruff check backend/ && backend/.venv/bin/ruff format --check backend/` → clean.
- [ ] **Step 3: Frontend** — `npx jest src app --silent` → 0 failures; `npx tsc --noEmit` → 0 errors.
- [ ] **Step 4: Live smoke** — restart uvicorn; re-run the Task 1 endpoint loop (no 500s), login with real credentials + ES256 token on an authenticated endpoint (200), and one full Playwright pass on the home → scan → skill-creator flow if the servers are up (optional but preferred).
